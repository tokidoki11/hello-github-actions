name: A workflow for my hello world
on: push
jobs:
  build:
    name: hello world action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: mona
